spring:
  cloud:
    propertiesvolume:
      prefixed: false
      enabled: true
      paths: /mnt/secrets/probate
      aliases:
        probate-notify-invite-apikey: services.notify.apiKey
        business-services-notify-invitedata-templateId: services.notify.invitedata.templateId
        business-services-notify-pin-templateId: services.notify.pin.templateId
        s2s-probate-backend: services.auth.provider.totp_secret
        AppInsightsInstrumentationKey: azure.app_insights_key